# Groq API Key (server routes / Vercel environment variables — stays private)
# Set GROQ_API_KEY in Vercel Settings > Environment Variables
GROQ_API_KEY=your_groq_api_key_here

# Frontend (Vite) variables
VITE_GROQ_API_KEY=
